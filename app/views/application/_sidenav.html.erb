<nav class="navbar navbar-default navbar-static-top <%= headerClass %>">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-left">
      <a class="navbar-brand" href="/"><%= image_tag "veterans_work_logo.png", height: "40", alt: "Company Logo" %></a>
    </div>
    <div class="navbar-right">
      <div class="nav open-nav" onclick="openNav()">&#9776;</div>
    </div>
  </div>

  <% if current_customer || current_company || current_admin %>
    <div id="mySidenav" class="sidenav">
      <a href="javascript:void(0)" class="closebtn" onclick="closeNav()">&times;</a>
      <a href="
        <% if current_customer %>
          /customers/edit
        <% elsif current_company %>
          /companies/<%=current_company.id%>
        <% end %>
      ">My Profile</a>
      <a href="/customer_requests">Customer Requests</a>
      <a href="/quotes">Quotes</a>
      <a href="/contracts">Contracts</a>
      <a>_________</a>
      <a href="#"><%= link_to "Logout #{current_company.name}", destroy_company_session_path, method: :delete if current_company %></a>
      <a href="#"><%= link_to "Logout #{current_customer.email}", destroy_customer_session_path, method: :delete if current_customer %></a>
      <a href="#"><%= link_to "Logout #{current_admin.email}", destroy_admin_session_path, method: :delete if current_admin %></a>
    </div>
  <% else %>
    <div id="mySidenav" class="sidenav">
      <a href="javascript:void(0)" class="closebtn" onclick="closeNav()">&times;</a>
      <a href="/">Home</a>
      <a href="/how">How It Works</a>
      <a href="/about">About</a>
      <a>_________</a>
      <div class="dropdown">
        <a class="dropdown-toggle" data-toggle="dropdown">Sign Up <span class="caret"></span></a>
        <ul class="dropdown-menu">
          <li><a href='/companies/sign_up' id='company-sign-up'>Company Sign-Up</a></li>
          <li><a href='/customers/sign_up' id='customer-sign-up'>Customer Sign-Up</a></li>
        </ul>
      </div>
      <div class="dropdown">
        <a class="dropdown-toggle" data-toggle="dropdown">Log In <span class="caret"></span></a>
        <ul class="dropdown-menu">
          <li><a href='/companies/sign_in' id='company-sign-in'>Company Sign-In</a></li>
          <li><a href='/customers/sign_in' id='customer-sign-in'>Customer Sign-In</a></li>
        </ul>
      </div>
    </div>
  <% end %>
</nav>

